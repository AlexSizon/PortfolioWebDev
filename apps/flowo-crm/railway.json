{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd /app && pnpm --filter flowo-crm build"
  },
  "deploy": {
    "startCommand": "cd /app/apps/flowo-crm && pnpm exec prisma db push --skip-generate && node scripts/bootstrap.mjs && node .next/standalone/apps/flowo-crm/server.js",
    "healthcheckPath": "/login",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
